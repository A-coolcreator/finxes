// Page composition entry for the case manager screen.
// Replace this with real DOM rendering or framework code when the UI is modularized.

export function initCaseManagerPage() {
  return {
    page: "case-manager",
    status: "ready",
  };
}
